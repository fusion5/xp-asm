default-extensions: 
  - StrictData
  - RecordWildCards
  - OverloadedStrings

dependencies:
  - base
  - haskell-say
  - text
  - containers
  - bytestring
  - binary
  - safe-decimal
  - transformers
  - extra
  - transformers

ghc-options:
  - -Wall
  - -Werror

library:
  source-dirs: 
    - src

executable:
  main: Main.hs
  source-dirs: 
    - src

tests:
  xp-asm-test:
    main: MainTest
    source-dirs: 
      - test
      - src
    dependencies:
      - hspec
        # - xp-asm
